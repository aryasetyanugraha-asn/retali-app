rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // --- Helper Functions ---

    // Cek apakah user sudah login
    function isSignedIn() {
      return request.auth != null;
    }

    // Mengambil data user dari database (jika Custom Claims belum diset)
    function getUserData() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data;
    }

    // Cek Role
    function isPusat() {
      return isSignedIn() && (request.auth.token.role == 'PUSAT' || getUserData().role == 'PUSAT');
    }

    function isCabang() {
      return isSignedIn() && (request.auth.token.role == 'CABANG' || getUserData().role == 'CABANG');
    }

    function isMitra() {
      return isSignedIn() && (request.auth.token.role == 'MITRA' || getUserData().role == 'MITRA');
    }

    // --- Rules per Collection ---

    // 1. Users Collection
    // Covers users/{userId} and all subcollections like users/{userId}/integrations/{doc}
    match /users/{userId} {
      allow read, write: if isSignedIn() && (request.auth.uid == userId || isPusat());
      // Allow Cabang to read Mitra data
      allow read: if isCabang() && resource.data.role == 'MITRA';

      match /{document=**} {
        allow read, write: if isSignedIn() && (request.auth.uid == userId || isPusat());
      }
    }

    // 2. Leads Collection
    match /leads/{leadId} {
      // PUSAT: Akses penuh
      allow read, write: if isPusat();

      // CABANG: Akses data milik cabangnya berdasarkan custom claims atau fallback
      allow read, write: if isCabang() && (
        (request.auth.token.branchId != null && resource.data.branchId == request.auth.token.branchId) ||
        (request.auth.token.branchId == null && resource.data.branchId == getUserData().branchId)
      );
      allow create: if isCabang() && (
        (request.auth.token.branchId != null && request.resource.data.branchId == request.auth.token.branchId) ||
        (request.auth.token.branchId == null && request.resource.data.branchId == getUserData().branchId)
      );

      // MITRA: Akses data yang ditugaskan padanya berdasarkan custom claims atau fallback
      allow read, write: if isMitra() && (
        (request.auth.token.partnerId != null && resource.data.partnerId == request.auth.token.partnerId) ||
        (request.auth.token.partnerId == null && resource.data.partnerId == request.auth.uid)
      );
      allow create: if isMitra() && (
        (request.auth.token.partnerId != null && request.resource.data.partnerId == request.auth.token.partnerId) ||
        (request.auth.token.partnerId == null && request.resource.data.partnerId == request.auth.uid)
      );
    }

    // 3. Content Requests (AI Jobs)
    match /content_requests/{jobId} {
      allow read, write: if isSignedIn() && resource.data.userId == request.auth.uid;
      allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid;
    }

    // 4. Posts (Scheduled Content)
    match /posts/{postId} {
      // Allow read, update, delete: if User owns the post
      allow read, update, delete: if isSignedIn() && resource.data.userId == request.auth.uid;
      // Allow create: if User creates post for themselves
      allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid;
    }

    // 5. Conversations Collection
    match /conversations/{conversationId} {
      // PUSAT: Akses penuh
      allow read, write: if isPusat();

      // CABANG: Akses data milik cabangnya berdasarkan custom claims atau fallback
      allow read, write: if isCabang() && (
        (request.auth.token.branchId != null && resource.data.branchId == request.auth.token.branchId) ||
        (request.auth.token.branchId == null && resource.data.branchId == getUserData().branchId)
      );
      allow create: if isCabang() && (
        (request.auth.token.branchId != null && request.resource.data.branchId == request.auth.token.branchId) ||
        (request.auth.token.branchId == null && request.resource.data.branchId == getUserData().branchId)
      );

      // MITRA: Akses data yang ditugaskan padanya berdasarkan custom claims atau fallback
      allow read, write: if isMitra() && (
        (request.auth.token.partnerId != null && resource.data.partnerId == request.auth.token.partnerId) ||
        (request.auth.token.partnerId == null && resource.data.partnerId == request.auth.uid)
      );
      allow create: if isMitra() && (
        (request.auth.token.partnerId != null && request.resource.data.partnerId == request.auth.token.partnerId) ||
        (request.auth.token.partnerId == null && request.resource.data.partnerId == request.auth.uid)
      );

      match /messages/{messageId} {
        allow read, write: if isPusat();

        allow read, write: if isCabang() && (
          (request.auth.token.branchId != null && get(/databases/$(database)/documents/conversations/$(conversationId)).data.branchId == request.auth.token.branchId) ||
          (request.auth.token.branchId == null && get(/databases/$(database)/documents/conversations/$(conversationId)).data.branchId == getUserData().branchId)
        );

        allow read, write: if isMitra() && (
          (request.auth.token.partnerId != null && get(/databases/$(database)/documents/conversations/$(conversationId)).data.partnerId == request.auth.token.partnerId) ||
          (request.auth.token.partnerId == null && get(/databases/$(database)/documents/conversations/$(conversationId)).data.partnerId == request.auth.uid)
        );
      }
    }

    // 6. Default Block
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
